const fs = require("fs"); const path = require("path"); const dicts = [ { name: "CET4", prefix: "cet4" }, { name: "CET6", prefix: "cet6" }, { name: "KAOYAN", prefix: "ky" } ]; for (const dict of dicts) { const out = []; for (let i = 0; i < 5500; i++) { out.push({ id: dict.prefix + "-" + i, word: dict.prefix + "_word_" + i, phonetic: "//", meanings: ["n. " + dict.name + " 测试词汇 " + i], category: dict.name }); } fs.writeFileSync(path.join(process.cwd(), "src/data/words_" + dict.name.toLowerCase() + ".json"), JSON.stringify(out, null, 2)); console.log("Saved " + dict.name); }